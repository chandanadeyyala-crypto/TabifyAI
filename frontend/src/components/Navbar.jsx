function Navbar(props){
    return(
        <div>
            <h1>{props.title}</h1>
            <p> This AI converts audio recordings into guitar tabs</p>
        </div>
    )
}

export default Navbar;